#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

if [[ -d "$ROOT/runs" ]]; then
    rm -rf "$ROOT/runs"
fi

echo "Removed generated mesh-study cases. Base case and plot scripts were preserved."
