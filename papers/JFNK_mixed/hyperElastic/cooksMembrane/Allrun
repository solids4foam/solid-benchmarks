#!/bin/bash

# Source required functions
. $WM_PROJECT_DIR/bin/tools/RunFunctions
source solids4FoamScripts.sh

BASE="base/hex.snes"
NAME="hex_snes"

usage()
{
    echo "Usage: $0 [LAST_MESH]"
    echo "  LAST_MESH must be an integer from 1 to 8 (default: 8)."
}

if [[ $# -gt 1 ]]
then
    usage >&2
    exit 2
fi

if [[ $# -eq 1 ]]
then
    if [[ ! "$1" =~ ^[1-8]$ ]]
    then
        echo "ERROR: LAST_MESH must be an integer from 1 to 8; received '$1'." >&2
        usage >&2
        exit 2
    fi

    # A command-line limit always means mesh 1 through LAST_MESH.
    START_MESH=1
    END_MESH=$1
else
    START_MESH=${START_MESH:-1}
    END_MESH=${END_MESH:-8}
fi

# Pressure stabilisation methods using the new stabilisation framework
stabilisations=(
    "STAB=rhiechow STAB_FILE=solidProperties.rhiechow"
    "STAB=laplacian STAB_FILE=solidProperties.laplacian"
    "STAB=jst STAB_FILE=solidProperties.jst"
    "STAB=evenlap_m0 STAB_FILE=solidProperties.evenlap_m0"
    "STAB=evenlap_m1 STAB_FILE=solidProperties.evenlap_m1"
    "STAB=evenlap_m2 STAB_FILE=solidProperties.evenlap_m2"
)

# Run only the m2 stabilisation by default. Set STAB_FILTER to another name, or
# to "all", to override this selection.
STAB_FILTER=${STAB_FILTER:-evenlap_m2}

# Mesh input files are defined from 1 to 8. Validate environment overrides too.
if [[ ! "${START_MESH}" =~ ^[1-8]$ || ! "${END_MESH}" =~ ^[1-8]$ \
      || "${START_MESH}" -gt "${END_MESH}" ]]
then
    echo "ERROR: mesh range must satisfy 1 <= START_MESH <= END_MESH <= 8." >&2
    exit 2
fi


# Detect the CPU type: we append this to the case name
if [[ "$OSTYPE" == "darwin"* ]]
then
    CPU_TYPE=$(sysctl -n machdep.cpu.brand_string 2>/dev/null | sed 's/[^a-zA-Z0-9]/_/g')
elif [[ -f /proc/cpuinfo ]]
then
    CPU_TYPE=$(grep -m 1 "model name" /proc/cpuinfo | awk -F': ' '{print $2}' | sed 's/[^a-zA-Z0-9]/_/g')
else
    CPU_TYPE="Unknown_CPU"
fi
[[ -z "${CPU_TYPE}" ]] && CPU_TYPE="Unknown_CPU"


# Create timestamped working directory for this run
DATE=$(date +%Y%m%d_%H%M%S)
RUN_DIR="run_${CPU_TYPE}_${DATE}"
echo "Creating ${RUN_DIR}"
mkdir "${RUN_DIR}"

# Enter the run directory
cd "${RUN_DIR}"

if [[ -n "$FOAM_USER_APPBIN" && -x "$FOAM_USER_APPBIN/solids4Foam" ]]
then
    SOLVER_EXE="$FOAM_USER_APPBIN/solids4Foam"
else
    SOLVER_EXE=$(which solids4Foam)
fi

echo "Using solver: ${SOLVER_EXE}"

for stabConfig in "${stabilisations[@]}"
do
    eval $stabConfig

    if [[ "${STAB_FILTER}" != "all" && "${STAB}" != "${STAB_FILTER}" ]]
    then
        continue
    fi

    echo; echo "***************************************"
    echo "Running pressure stabilisation: ${STAB}"
    echo "***************************************"

    SUMMARY="${NAME}__${STAB}.summary.txt"
    ITERATIONS="${NAME}__${STAB}.iterations.txt"
    echo "# Mesh Time Mem D" > "${SUMMARY}"
    echo "# Mesh SNESIts MaxLinearIts TotalLinearIts" > "${ITERATIONS}"

    for i in $(seq "${START_MESH}" "${END_MESH}")
    do
        CASE="${NAME}__${STAB}.${i}"
        echo; echo "Processing case: $CASE"

        cp -r "../${BASE}" "${CASE}"
        cd "$CASE"

        if [[ ! -f "constant/${STAB_FILE}" ]]
        then
            echo "ERROR: missing constant/${STAB_FILE} in $(pwd)"
            exit 1
        fi
        cp "constant/${STAB_FILE}" constant/solidProperties

        if [[ ! -f "system/blockMeshDict.$i" ]]
        then
            echo "Cannot find ${CASE}/system/blockMeshDict.$i"; echo
            exit 1
        else
            echo "Copying ${CASE}/system/blockMeshDict.$i to ${CASE}/system/blockMeshDict"
            cp "system/blockMeshDict.$i" system/blockMeshDict
            rm -f constant/polyMesh/blockMeshDict
        fi

        solids4Foam::runApplication blockMesh

        if command -v gtime &> /dev/null
        then
            echo "Running solids4Foam on ${CASE} with gtime"
            gtime -f "Maximum resident set size (kbytes): %M" "$SOLVER_EXE" &> log.solids4Foam
            SOLVER_STATUS=$?
        elif command -v /usr/bin/time &> /dev/null && /usr/bin/time -f "Maximum resident set size (kbytes): %M" true &> /dev/null
        then
            echo "Running solids4Foam on ${CASE} with /usr/bin/time"
            /usr/bin/time -f "Maximum resident set size (kbytes): %M" "$SOLVER_EXE" &> log.solids4Foam
            SOLVER_STATUS=$?
        else
            echo "Running solids4Foam on ${CASE}"
            "$SOLVER_EXE" &> log.solids4Foam
            SOLVER_STATUS=$?
        fi

        if [ "$SOLVER_STATUS" -ne 0 ]
        then
            echo "solids4Foam failed for ${CASE}; see ${PWD}/log.solids4Foam"
            exit "$SOLVER_STATUS"
        fi

        echo; echo "Appending results to ${SUMMARY}"
        if grep -q "Maximum resident" log.solids4Foam
        then
            MAX_MEMORY=$(grep "Maximum resident" log.solids4Foam | awk '{print int($6 / 1000)}')
        else
            MAX_MEMORY="NaN"
        fi

        CLOCK_TIME=$(grep "ClockTime" log.solids4Foam | tail -n 1 | awk '{print $7}')
        DISP=$(tail -1 postProcessing/0/solidPointDisplacement_pointDisp.dat | awk '{print $3}')

        echo "$i $CLOCK_TIME $MAX_MEMORY $DISP" >> ../"${SUMMARY}"

        SNES_ITS=$(grep "Nonlinear solve converged" log.solids4Foam | tail -n 1 | awk '{print $NF}')
        read MAX_LINEAR_ITS TOTAL_LINEAR_ITS <<< \
            $(awk '/Linear solve/ && /iterations/ {sum += $NF; if ($NF > max) max = $NF} END {print max+0, sum+0}' log.solids4Foam)

        echo "$i $SNES_ITS $MAX_LINEAR_ITS $TOTAL_LINEAR_ITS" >> ../"${ITERATIONS}"

        cd ..
    done

    echo; echo "${SUMMARY} file:"; cat "${SUMMARY}"; echo
    echo; echo "${ITERATIONS} file:"; cat "${ITERATIONS}"; echo
done

# Plot the present m2 results alongside the reference results after all
# requested cases have finished.
if [[ "${RUN_PLOTS:-true}" == "false" ]]
then
    echo "Skipping plots because RUN_PLOTS=false"
elif ! command -v gnuplot &>/dev/null
then
    echo "WARNING: gnuplot is not installed; skipping plots"
else
    PLOT_SCRIPT="stabVsLiterature.gnuplot"
    cp "../plotScripts/${PLOT_SCRIPT}" \
       ../plotScripts/deal.II.dat \
       ../plotScripts/abaqus.dat .

    echo "Running gnuplot on ${PLOT_SCRIPT}"
    gnuplot "${PLOT_SCRIPT}"
fi

# Exit the run directory
cd ..

echo; echo; echo "Done!"; echo
echo "View the PDF files in ${RUN_DIR}"
echo; echo "Completed at" $(date +%Y%m%d_%H%M%S)
echo; echo
